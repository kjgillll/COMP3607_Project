/** 
 * The CourseListing interface describes the common behaviours 
 * shared between the Department and Courses Classes. 
 */
public interface CourseListing{ 
    public boolean getAvailable(Student student); 
}//end CourseListing()